# mk-cc-resources — briefing (2026-08-23)
Ship: Phase 1 status spine SHIPPED + INSTALLED, verified live — the [instr] line fired in a real injection. status.json ledger runs this ship; done/-moves retired here; all push gates green.
Last: four plugin bumps (steward/kb/statusline/turn-end) + contract page design/status-contract.md; pilot ledger seeded by backfill.
Next: 1. Dogfood week — staleness accuracy, fallback fires (trace engine field), ledger truth, statusline; gates Phase 2 fleet rollout (#12). 2. Ratify distribution [you]. 3. Autopilot decide().
Waiting: Q13 steward-on-sonnet (default: try it) · Q12 CI revert (default: deliberate).
