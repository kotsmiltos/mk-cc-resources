# mk-cc-resources — briefing (2026-09-12)
Ship: `@fc` — the ninth modifier and the first mechanism for your no-pointers law — is BUILT but not committed, sitting on top of five pushed-and-uninstalled ships: nothing built this week reaches a running process yet.
Last: a ten-commit-stale base wasted a whole build, so `git fetch` is now step 0 of any bump (#41); the exec ledger was caught cutting commands at 300 chars and falsely refuting two gates that had passed (#42, ahead of Phase 2); #9's flake is the sweep discarding its own evidence.
Next: rule Q25 — push `@fc`? (default: yes, keep the law on demand) — then `claude plugin update` + RESTART.
Waiting: Q25 @fc · Q24 install layout · Q17 · Q21 · Q22 · Q20 · Q14 · Q13 · Q12.
