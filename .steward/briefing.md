# mk-cc-resources — briefing (2026-09-18, garden pass)
Ship: the garden (steward 0.7.0) shipped AND just ran for real — every model file cut to cap by deletion under the owner's one-live-copy ruling; git history is now the archive.
Last: kb-hints off by default (kb 0.16.0) landed; the 300s judge timeout raised but did NOT fix empty picks (42–81% across ships); #9's flake named (lock contention, fix not applied).
Next: re-measure push bytes + judge empty-pick rate after 20 real prompts (Q26); land #42 parts 2–4 before #32's goal duty.
Waiting: Q26 (narrowed) · Q22 · Q20 · Q17.
