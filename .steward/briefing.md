# mk-cc-resources — briefing (2026-08-23)
Ship: PUSHED — origin == local 28cc0c7; whole backlog public (self-check 0.4.0/0.4.1, steward diet 0.3.0/0.3.1, request-closure 0.5.0). Installs still on 0.4.0/0.3.0 until update + restart.
Last: your 08-10 wish shipped same sitting — wake-turns must END answering YOUR request + who-did-what, not the last agent (request-closure duty, advise; every wake-yield renudges). Tests 143/143.
Next: 1. update installs + restart, watch live: self-check ladder · steward-sync first fire · one wake-turn ending on your request · lighter session-open text. 2. Ratify distribution [you]. 3. Autopilot decide() extraction.
Waiting: Q13 steward-on-sonnet (default: try it) · Q11 recall re-take (default: keep) · Q12 CI revert (default: deliberate).
