{
  "description": "Detects prompt-modifier keywords (++/@thorough, @ship, @present, @debug, @verify, @fresh, @prompt, @build, @fc) and injects the matching behavioural protocol; also hints the modifier when the intent is typed without the keyword. Stands down on machine-authored prompts.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/thorough-mode.js\""
          }
        ]
      }
    ]
  }
}
