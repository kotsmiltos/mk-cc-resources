# Override repo-level lib/ ignore — essense-flow needs its lib/ tracked
!lib/
!node_modules/**/lib/

# Override repo-level build/ ignore — the /build skill lives in skills/build/
# and would otherwise be swallowed by the Python-distribution build/ rule.
!skills/build/
