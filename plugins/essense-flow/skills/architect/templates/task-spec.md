# GENERATED from references/schemas/task-spec.schema.yaml — edit the schema, then: npm run render-schemas
schema_version: 1
task_id: {{task_id}}
module: {{module_name}}
goal: "{{goal}}"
requirements_traced: {{requirements_traced}}
file_write_contract:
  paths: {{paths}}
  out_of_contract: {{out_of_contract}}
  scratch_space: {{scratch_space}}
behavioral_pseudocode: |
{{behavioral_pseudocode}}
test_completion_contract: {{test_completion_contract}}
dependencies: {{dependencies}}
exposes: {{exposes}}
consumes: {{consumes}}
agency_level: {{agency_level}}
agency_rationale: "{{agency_rationale}}"
