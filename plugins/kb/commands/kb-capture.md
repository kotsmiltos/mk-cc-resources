---
description: File one piece of knowledge into the project KB now — a decision with its why, a dead end, a finding, a convention. One dated file, queryable immediately.
---

Invoke the `kb-capture` skill with: $ARGUMENTS

If $ARGUMENTS names the fact, distill and file it (routing rule: steward-model changes go to
.steward/inbox/ instead). If empty, ask what to capture in one line. Confirm with
"Captured to kb: <title> (kind)." and return to the work.
