---
description: Seed the knowledge base from THIS existing project — sweep docs, git history, ledgers, and code IN DEPTH; extract decisions, conventions, and findings into .claude/kb/extracted/ (seeder judges autonomously, then shows the owner what landed).
---

Invoke the `kb-seed` skill for this project. Target hint from the owner (optional): $ARGUMENTS

Follow the skill exactly: inventory ALL substrate rows in depth → distill → judge and write
dated, sourced files with frontmatter → REPORT what landed (owner prunes after the fact) →
verify via `kb stat` + one probe query, and report both.
