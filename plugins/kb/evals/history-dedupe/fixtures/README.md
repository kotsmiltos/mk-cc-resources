# mk-cc-resources (sandbox slice)

A Claude Code plugin marketplace: each directory under `plugins/` is a separately versioned,
independently installable plugin. Only the six hook scripts are present in this sandbox. The
project's knowledge base lives under `.claude/kb/`.
