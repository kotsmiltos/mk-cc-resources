---
# Did the session reach the capture that answers this? The pull hint (WITH kb) names it; a
# WITHOUT session finds it only by searching the knowledge base itself. Graded on the trace: a
# Read of the file, or the hint naming it, both carry its file name.
type: regex
pattern: 'cross-plugin-duplication-is-correct-do-not-extract'
target: trace
---
