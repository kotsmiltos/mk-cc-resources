# mk-cc-resources (sandbox slice)

A Claude Code plugin marketplace. Hook scripts live under `hooks/scripts/` and are registered in
`hooks/hooks.json`. The project's knowledge base lives under `.claude/kb/`.
