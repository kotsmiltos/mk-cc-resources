# Code glossary — {{target_path}}

Generated: {{generated_at}}
Scope: {{scope_summary}}
Skill version: {{skill_version}}

## Summary

| Metric | Value |
|--------|-------|
| Functions indexed | {{total_functions_indexed}} |
| Canonical clusters | {{total_clusters}} |
| Extractable clusters (≥2 instances + variant axis) | {{total_extractable}} |
| Language mix | {{language_mix}} |

## Top extractables

Sorted by instance count (descending). High-value DRY candidates.

{{#each extractable_clusters}}
### {{id}} — {{name}}

**What it does:** {{description}}

**Instances:** {{instance_count}} across {{file_count}} files

**Proposed extraction:**

```
{{canonical_signature}}
```

**Target home:** `{{proposed_module}}`

**Invariant skeleton:**

```
{{invariant_skeleton}}
```

**Variant axis:**

{{#each variant_axis}}
- `{{parameter}}` ({{inferred_type}}): {{instance_values}}
{{/each}}

**Call sites:**

{{#each instances}}
- `{{file}}:{{line}}` — `{{function}}` ({{variant_values}})

  ```
  {{body_excerpt}}
  ```

{{/each}}

{{#if related_functionalities}}
**Related:** {{related_functionalities}}
{{/if}}

{{#if notes}}
**Notes:** {{notes}}
{{/if}}

---

{{/each}}

## Non-extractable clusters

Functionalities present once, or labelled but not extractable. Useful as a watch-list — if a second instance appears later, this entry promotes.

{{#each non_extractable_clusters}}
- **{{id}} — {{name}}** ({{instance_count}} instance{{plural}})
  - `{{first_instance_file}}:{{first_instance_line}}` — `{{first_instance_function}}`
  - Notes: {{notes}}
{{/each}}

## Block-level secondary findings

Duplicated sub-function blocks (3+ lines, ≥2 occurrences) that don't map cleanly to a function cluster.

{{#each block_clusters}}
- **{{id}} — {{name}}** — {{instance_count}} instances
  - {{pattern_summary}}
  - Instances: {{instance_summary}}
{{/each}}

## Next steps

1. **Review the top extractables** — verify the proposed signatures match how you'd want to refactor.
2. **Check `proposed_module` paths** — does the target home already exist? Confirm new dirs are intentional.
3. **Spot-check `body_excerpt` quotes** — open one cluster's call-sites and confirm the verbatim matches your code.
4. **Extract** — manually, or feed `GLOSSARY.yaml` to a future `/dry-refactor` skill (v2). Either way, run your test suite after each extraction.
5. **Re-run `/code-glossary`** after extraction to confirm the cluster collapses to one canonical instance + N call-sites of the new shared helper.
