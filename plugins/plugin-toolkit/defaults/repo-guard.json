{
  "detectors": {
    "leaked-path": {
      "_allow_note": "Each entry is a FILE that must contain drive-shaped strings to do its job: detector source, its tests, and suites whose fixtures exercise Windows path handling. Never allowlist a directory that is a proven leak site \u2014 .steward/ was exempted here once and removed, because leaked-path's own header names it as a place the class hid. Known debt, NOT exempt by design: plugins/essense-flow/test/ carries the author's real paths as load-bearing literals (a blanket replace broke 4 suites and was reverted) \u2014 remove this entry when that pass lands. 2026-08-27: five entries added after the first ROOT-scoped run since 08-23 (the documented toolkit-cwd invocation scans only plugin-toolkit, so these sat invisible): turn-end/steward doc comments + fixtures explaining the win32 case-insensitive home-boundary guard with the ellipsized shape c-colon-users (no real username), and patterns' state-hash case test \u2014 all the sanctioned exercise-Windows-paths class. 2026-09-11: `plugins/turn-end/RELEASE-NOTES.md` became `design/notes/turn-end-history.md` in the CHANGELOG migration, and the allowlist is path-keyed, so the same sanctioned line resurfaced as a blocking finding at its new path \u2014 the entry MOVED with the text, it was not widened. Worth keeping in mind: moving a file re-arms every path-keyed exemption it carried, which is the guard behaving correctly.",
      "allow": [
        "plugins/essense-flow/test/",
        "plugins/plugin-toolkit/lib/detectors/leaked-path.js",
        "plugins/plugin-toolkit/tests/repo-guard.test.js",
        "plugins/kb/tests/kb-scribe.test.js",
        "plugins/statusline/tests/mk-statusline.test.js",
        "plugins/patterns/tests/patterns.test.js",
        "plugins/steward/hooks/scripts/steward-brief.js",
        "plugins/turn-end/hooks/scripts/turn-end.js",
        "plugins/turn-end/tests/turn-end.test.js",
        "design/notes/turn-end-history.md"
      ]
    },
    "silenced-failure": {
      "allow": []
    },
    "revert-chain": {
      "minRunLength": 3,
      "windowMinutes": 60,
      "allow": []
    }
  }
}
