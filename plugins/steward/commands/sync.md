---
description: Integrate the inbox into the project model NOW and show the diff — what your captured thoughts changed in the plan, and why.
---

Dispatch the `steward` agent (job: integrate) over `.steward/inbox/` plus anything notable said
this session that wasn't yet captured. Show the owner the returned diff in full — added / edited /
deleted / reordered, each with its one-line why — and the refreshed next-3 tasks. If the inbox is
empty and nothing changed, say exactly that in one line.
