---
name: mk-flow-update-rules
description: "Deprecated: use /mk-flow-update instead. This skill only synced rules.yaml; the new /mk-flow-update syncs rules, intents, and cross-references."
---

<objective>
This skill has been replaced by `/mk-flow-update`, which syncs all context files (rules, intents, cross-references) — not just rules.

Run `/mk-flow-update` instead.
</objective>

<quick_start>
Tell the user: "This command has been replaced by `/mk-flow-update`, which syncs all context files (rules, intents, cross-references). Running that instead."

Then execute the mk-flow-update skill at `plugins/mk-flow/skills/mk-flow-update/SKILL.md`.
</quick_start>
