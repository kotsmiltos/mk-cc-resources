{
  "_why": "eval fixture: only the two duties under test run; context-recall would spawn a claude child, the others are inert in a bare cwd but are switched off explicitly",
  "duties": {
    "context-recall": { "enabled": false },
    "session-digest": { "enabled": false },
    "steward-sync": { "enabled": false },
    "quality-lens": { "enabled": false },
    "request-closure": { "enabled": false },
    "fewer-clicks": { "enabled": false }
  }
}
