# mk-cc-resources — the page

Rewritten whole at the end of every sitting. States only what IS. History lives in git and
`DECISIONS.md`. Under 100 lines. (Owner ruling 2026-09-18: subtract — one page, one decisions
list, one Stop question.)

## What this is

The owner's Claude Code plugin marketplace: the toolkit that lets them chat freely with Claude,
have a vision built into well-made software, and have progress captured. Public repo; built
first for the owner's own ~40 production codebases.

## Laws (must stay true)

1. No work in the owner's absence.
2. Quality over speed. A mechanism that silently delivers nothing is a quality failure.
3. Mechanisms, not text. Deterministic before LLM. Fold before add. Fire conditionally.
4. Recompute, never accrete. This page is rewritten, never appended.
5. Never an unverified "done": every work turn names the check that proved it.
6. In-environment delivery, fewest clicks: content in the terminal, decisions as one-keystroke
   choices with the default first, never a file to read.
7. Nothing personal in shipped files (repo-guard's `leaked-path` enforces it).
8. Decoupled, open-for-extension code; measured per project, never across plugins.
9. **A kickoff prompt carries the owner's words verbatim, names what it adds, and states its
   cost; it never grants itself "ask nothing"** (thorough-mode 1.14.0: the `@prompt` contract +
   the receiving-side `[kickoff-guard]`, one keystroke before the first sub-agent dispatch).

## Where we are (2026-09-20, end of sitting)

- `main` is ahead of origin and NOT pushed (count: `git rev-list --count origin/main..HEAD`).
- **The owner's ask:** test building a simple webapp from different angles, with and without the
  plugins. **What happened:** the morning kickoff (generated by `@prompt`, in the session's voice,
  "ask nothing") ran essense-flow's ten-phase pipeline on `postit-board` for 2h40m / ~50 opus agents /
  ~9M tokens and produced 6 of 32 modules, no app, before the owner stopped it. Record: the
  `postit-board` repo, committed locally, never pushed. Fix shipped: thorough-mode 1.14.0 (kickoff
  contract + `[kickoff-guard]`, law 9). The kickoff file is deleted.
- **The measure as meant** (`plugin-eval`, case `postit-board`, one shot, opus, 1 run/arm, no shell
  in the arm; every arm booted and pulled the owner's real sessions afterwards):

  | plugin | WITH | W/OUT | Δ | what moved |
  |---|---|---|---|---|
  | steward | 0.75 | 0.75 | 0 | inbox capture 1/1 vs 0/1 (its one job); `node:http` miss is a grader artifact |
  | turn-end | 0.80 | 1.00 | −0.20 | WITH hit the 60-turn cap and never yielded; BOTH arms rewrote the page because the fixture page says "rewritten whole" |

  Deterministic scan of the four arms (+ the pipeline stub): features 10/10, 10/10, 9/10, 10/10
  (pipeline 4/10); house rules held in all four unaided (atomic write, one guard, 0 packages,
  0 silent catches, 0 port literals); code-glossary: 0 reach-ins everywhere, one import cycle in
  three arms, clones 7–17 clusters (worst: the same `stop()` in eleven managers). Cost $5–6 and
  17–21 min per arm. n=1: the behavioural differences (a broken `ai-title` parse, a missing fenced
  export) are dice, not verdicts.
- **What it tells us:** (1) the WHAT did the work — a plugin cannot improve code the prompt already
  specifies; (2) plugins move only what lives outside the code, and only their one job; (3) a
  convention written in the repo did turn-end's page duty for free — the hook added a turn cap;
  (4) the pipeline is the wrong tool for anything one agent can hold in context — its lenses fixed
  seam bugs the split itself created; (5) the shared gap in all four arms is that nothing RAN its
  own code — the largest measured effect so far (`@verify` 3/3 vs 0/3) is exactly "run it and
  prove it"; (6) regex graders decided both Δ rows, not the plugins.
- Six sonnet measures from 09-19 stand: turn-end +38, steward +33, kb +27, patterns +17, `@verify`
  3/3 vs 0/3, reuse-gate 0, `++` retired.
- Running here: turn-end (page duty only), thorough-mode, prism, elicit, plugin-toolkit, caveman,
  statusline, alert-sounds. Off here: kb, steward, lens, patterns, reuse-gate, essense-flow,
  autopilot, session-lifecycle.

## Next (each with its check)

1. **Fix the instrument before measuring again:** behaviour probes per arm (create → drag → edit
   → done → export one fenced block → kill → restart → read back; pull returns a real session
   title) in place of regex graders, and 3 runs per arm. Check: the probe script runs green
   against the steward/without arm on disk.
2. **The other angles, same case, one shot, opus:** kb, patterns, reuse-gate, `@verify`; and
   turn-end again with the page rule in the fixture PROJECT.md but the hook OFF, to test finding (3).
   Check: one row per plugin, n=3, on this page.
3. **Decide essense-flow's place:** park it for anything one agent holds in context, or name the
   size where it earns its coordination cost. Check: one dated DECISIONS line.
4. **Live with the subtraction for five sittings here.** Check: this page rewritten each sitting.
5. **Decide push + fleet.** Check: each fleet repo has a `PROJECT.md` under 100 lines.

## Open decisions (default first)

- **Q17 — the five dead hooks** (essense-flow, essense-autopilot, reuse-gate, patterns, serena
  read-guard): disabled here now; disable globally? Default: yes for reuse-gate (measured +0);
  patterns measured +17 — keep it available per project.
- **Global prompt hooks** (`verification-rules.js`, `generalize-first.sh` fire on every prompt):
  fold their lines into the global CLAUDE.md and remove the hooks? Default: yes.
- **The recall judge**: ranker-only, or off with the rest? Default: off — the page is the recall.
